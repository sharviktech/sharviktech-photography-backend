SHARVIK API

Instructions:
1. Navigate to this folder.
2. Run `dotnet restore` then `dotnet ef database update` to create the database (ensure EF tools installed and configured).
3. Run `dotnet run` to start the API.

Default connection string uses localdb; update `appsettings.json` to point to your SQL Server instance if needed.
